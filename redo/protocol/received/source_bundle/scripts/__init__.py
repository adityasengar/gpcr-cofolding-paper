"""paper_af3 helper scripts (spawn_experiment, rescore_experiment, etc).

Made a package so console-script entry points in pyproject.toml can
target ``scripts.<module>:main`` — pip installs the CLI wrappers under
the venv's bin/ that way.
"""
