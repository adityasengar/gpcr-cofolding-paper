% =====================================================================
% main.tex — the manuscript container.
%
% OWNED BY THE ORCHESTRATOR. Section CONTENT lives in sections/*.tex and is
% written by the section's author; this file is the container and house style.
%
% Submission format follows Nature Communications guidance: single column,
% double spaced, continuous line numbers, numbered citations.
% Build:  ./manuscript/build.sh
% =====================================================================
\documentclass[11pt,a4paper]{article}

% --- encoding and fonts -------------------------------------------------
\usepackage[T1]{fontenc}      % renders accented author names (Söding, Šali, Pándy-Szekeres)
\usepackage[utf8]{inputenc}
\usepackage{mathptmx}         % Times text and maths — the journal house look
% NOTE: helvet/courier omitted deliberately — BasicTeX ships the .sty but not the
% URW font metrics (phvr8t), so loading them breaks the build on a clean machine.
\usepackage{microtype}        % subtle spacing; makes justified text much cleaner

% --- cross-references into the SI --------------------------------------
% main.pdf and si.pdf are SEPARATE documents, so a bare \ref to an `sfig:'
% label printed as ?? in the main text. Eight of them did, undetected, because
% build.sh reports undefined CITATIONS and the undefined-reference warnings sat
% in main.log unread. xr imports si.aux so those resolve to the SI's own S-
% numbering. It requires si.aux to exist, which is why build.sh now builds the
% SI FIRST. xr.sty ships in `tools', which is in tex/tex-packages.txt.
\usepackage{xr}
\externaldocument{si}

% --- page geometry ------------------------------------------------------
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{setspace}
\doublespacing                % submission format; comment out to read comfortably
\usepackage[modulo]{lineno}   % reviewers cite line numbers
\linenumbers

% --- headings -----------------------------------------------------------
\usepackage{titlesec}
\titleformat{\section}{\normalfont\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{1em}{}
\titlespacing*{\section}{0pt}{1.6em}{0.6em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.4em}

% --- floats, tables, links ---------------------------------------------
\usepackage{graphicx}
% Panels live in figures/out/, which is gitignored because it is regenerable:
% run figures/block_a/panels/*.py. build.sh names any that are missing.
\graphicspath{{../figures/out/}}
\usepackage{booktabs}
% longtable, added 2026-09-10. S-T1 and S-T2 are generated as longtables rather
% than table+tabular because S-T2 is 192 rows and a tabular cannot break across
% a page -- it compiled to a float 1509 pt tall and silently ran off the bottom,
% losing most of its rows. See analysis/block_a/make_tables.py.
\usepackage{longtable}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[hidelinks,breaklinks=true]{hyperref}

% --- running head -------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}\fancyhf{}
\fancyhead[R]{\footnotesize\itshape Conformational-state control in co-folding models}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% --- title block --------------------------------------------------------
\usepackage{authblk}
\renewcommand\Affilfont{\small}
\setlength{\affilsep}{0.6em}

% TITLE SCOPE, corrected 2026-09-13. This read "A Ga alpha5 C-terminal PEPTIDE
% co-input drives..." -- a scope no block evidences: no arm in Blocks A-D supplies a
% peptide of ANY length, every partner arm is a complete Ga subunit or a nanobody,
% and the segment actually manipulated is ELEVEN residues. si.tex was corrected on
% 2026-09-12 and THIS WAS NOT, which is the same failure one level up: the SI got
% fixed and the paper's own title kept the claim. CLAUDE.md names this class; the
% project has now written it down four times and gone on committing instances.
% The numeral returns to the title only if the length ladder runs (PLAN.md).
\title{\bfseries A G\texorpdfstring{$\alpha$}{alpha} subunit supplied as a co-input
  drives GPCR co-folding models into the active state}

\author[1]{Aditya Sengar}
\affil[1]{Novartis Biomedical Research}
\date{\today}

\begin{document}
\maketitle
\thispagestyle{fancy}

\begin{abstract}
\noindent
\textit{Abstract to be written once the Results are landed. See \texttt{CLAIMS.md}
for the claims it must compress. Block A is groundwork: the instrument, the
whole-partner effect at panel scale, and what the models do not do.}
\end{abstract}

\begin{figure}[htbp]\centering
\includegraphics[width=\textwidth]{ga1_hero.pdf}
\caption*{\textbf{Graphical abstract.}
\textbf{a}, a receptor predicted from sequence alone: TM6 closed, predicate
inactive. \textbf{b}, a receptor predicted with its cognate G$\alpha$: TM6 open,
predicate active, with the $\alpha$5 C-terminal helix of the supplied
G$\alpha$ in the intracellular cavity. \textbf{The whole subunit is the input};
only its C-terminal helix is drawn, so that the cavity stays visible. \textbf{c}, that prediction on the deposited active
structure, viewed from the cytoplasm. Every distance is measured on the
coordinates drawn and is annotated with the atom pair it was measured between.
\textbf{d}, all 7{,}166 Class~A predictions with both axes measurable, on the
predicate plane, with the deposited references overlaid and \textbf{the two rows
rendered above ringed on their own points}. \textbf{e}, 159 receptor $\times$
backbone cells, apo against cognate. Panels \textbf{a} and \textbf{b} are
different receptors: the archive ships one prediction per case, and the
within-receptor contrast is in \textbf{d} and \textbf{e}. This figure shows
state reached; it does not show amplitude reproduction, which is absent on three
of four backbones (Fig.~\ref{sfig:amplitude}, Table~\ref{tab:t3_amplitude_fits}).}
\end{figure}
\clearpage

\input{sections/intro}
\input{sections/results}
\input{sections/methods}

\clearpage
\section*{Figures}
\input{sections/figures}

\clearpage
\section*{Tables}
\input{tables/T1_panel_composition}
\input{tables/T2_headline_by_backbone}
\input{tables/T3_amplitude_fits}
\input{tables/T4_confidence}
\input{tables/T5_exclusion_sweep}

\bibliographystyle{unsrtnat}
\bibliography{refs}

\end{document}
