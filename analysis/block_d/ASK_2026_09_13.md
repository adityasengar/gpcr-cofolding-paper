# Block D — what we still need to finish the reproduction

**To:** `paper_af3`
**From:** the manuscript side (paper-6f), 2026-09-13
**Supersedes:** `analysis/block_d/DATA_REQUESTS.md` (2026-09-10), which had twelve
items. Four of those twelve are withdrawn below because your own bundle already
answers them, and one because we finished it ourselves this morning.

Cost classes, as in the earlier documents: **free** = re-analysis or re-send of
something that already exists on your side · **cheap** = re-scoring existing
predictions, no new inference · **real** = new predictions.

**There is no `real` item anywhere in this document.** Nothing here asks you to run
anything. Everything below either exists on your disk today or is a sentence.

---

## Where we got to without the rows, so you can see what the ask is actually for

Block D's documentation is the most self-critical of the four drops, and a lot of it
holds up under independent check. Since intake we have recomputed, from the bundle
alone: the three panel sizes (7 / 4 / 26), the 22 paralog clusters over 26 D3
receptors, both cluster-boot degeneracy statements, all six Clopper–Pearson intervals
at n = 50 to the stated decimal, the ln-versus-log₁₀ factor behind Flag D-2, the four
nanobody anchor identities, the cutoff comparison on the three dated backbones, and
fifteen NPxxY-hydroxyl distances measured directly from the shipped CIFs. Forty-nine
of fifty-four checks reproduce and the five that do not are all sub-Ångström and change
no state call.

Three things we settled since, which shorten this document:

- **All three corpus totals reconcile exactly against their nominal grids**, using
  your headline documents' own job counts. D1: 7 × 4 × 5 seeds × 100 samples = 14,000,
  140/140 jobs landed. D2: 12 receptor-arm combinations × 4 backbones × 5 seeds ×
  10 samples = 2,400 nominal, 237/240 jobs landed → 2,370, with the shortfall on
  `AGTR1 × active_nb × of3` (n = 20, the three killed OF3 stragglers). D3: 26 × 4 × 5
  depths × 5 seeds × 10 = 26,000 nominal, 19 jobs short → 25,810, matching C-D-6's
  per-backbone split. **We are not asking you to explain a shortfall in any tier.**
- **SC-D-11 now reproduces end-to-end on our side.** `PARTA_D1.md §6` names the source
  and the filter precisely enough to re-run — Block A rows, 7 D1 receptors, apo arm,
  n = 25 per receptor × backbone cell. Applied to the Block A rows we hold, that filter
  selects exactly 700 rows, exactly 25 per cell, and the two-instrument predicate
  reproduces **all 28 cells to the decimal** and all four cross-tier deltas
  (+96.0, +52.0, +96.0, +88.0). Block D's strongest claim is now the one Block D claim
  we can verify from data in hand.
- **The D2 grid has no unexplained gap.** `W-D-9` names the ADRB2 × active_nb drop and
  its cause; with that arm out, 12 × 4 = 48 cells, all populated. Our 2026-09-10
  document said seven cells were "named nowhere". That was our arithmetic error, not a
  gap in your bundle, and it is withdrawn.
- **SC-D-2 reproduces from the D1 headline's own counts.** The three convergent-inactive
  receptors top out at NPY1R × OF3, 33/500 = 6.6 %, with CXCR4 at 3.8 % and GHSR at 0.0
  on every backbone. That is the stated ceiling exactly.

What remains is that **no headline fraction in any tier can be recomputed from a row
table here**, because none of the three shipped. Our verifier carries those claims as
PROSE-ONLY — a third class beside RECOMPUTED and CONSISTENCY — so that a pass count can
never be read as a claim count. It holds twelve such entries over nine of the twelve
SC-D claims as it stands; SC-D-11 and SC-D-2 come out of that class today on the two
findings above, leaving ten entries over seven claims.

---

# Rank 1 — the three row tables

## 1. `rows.csv` for D1, D2 and D3. **free**

```
experiments/022_tier_d1_deep_apo/analysis/full/rows.csv          14,000 rows  (14,001 lines, ~30 MB)
experiments/023_tier_d2_directed_inactive/analysis/full/rows.csv  2,370 rows  (~5.3 MB)
experiments/024_tier_d3_msa_depth/analysis/full/rows.csv         25,810 rows
```

Sizes as stated in your own `tier_d1_full_headline_2026_09_06.md` and
`tier_d2_full_headline_2026_09_07.md`; the D3 headline gives the row count but not a
size.

**How we will recognise them.** We hold no sha256 for any of the three — unlike
`rows.tier3.v2.csv`, which was pinned in eighteen places and matched on arrival, these
three are named by path only. What we do hold, and will check every row against:

| pin | value | source |
|---|---|---|
| `scorer_git_sha` | `d9c646af5f89861c16062bf256de96a8389d9915` | claim sheet header, `BLOCK_D_STATE_CHECK.md` Q2, GATE-4 |
| `ref_set_csv_sha256` | `7a261988ff73eedc9d21e5cc2a9eaf0436b9f38e3827a87493295a8d8382b90c` | `BLOCK_D_STATE_CHECK.md` Q2 and Q4, uniform on all 42,180 rows |
| `chain_selection_method` | `identity_match_to_wt` | `BLOCK_D_STATE_CHECK.md` Q3, uniform on all 42,180 rows |
| thresholds as applied | `9.08` / `14.932` | field 81 / 82 of the sample row you sent |
| per-cell reproduction | the D1 headline's 28 k/n pairs; GATE-2 §2's 20 (backbone × depth) fractions | both are exact targets |

Your three paths are also pinned inside the bundle itself: `MANIFEST.json`'s
`source_rows_csv` key names all three, and `ASSUMED_NOT_VERIFIED_BLOCK_D.md` describes
the release convention as `data/block_d/{d1,d2,d3}/rows.csv` — so the in-bundle
location is already decided on your side.

**What each unblocks.** Ranked by what a manuscript sentence depends on:

| claim | what we cannot currently check | needs |
|---|---|---|
| SC-D-1 | the four F1 outlier deltas (see item 3 — the claim sheet and Part A disagree) | D1 |
| SC-D-3 | CNR2 100 % sub-Å to **both** references on all four backbones — the saturation result that licenses preferring the two-instrument predicate over RMSD | D1 + `pocket_ca_rmsd_active` / `_inactive` |
| SC-D-5 | the ACM2 apo → active_nb deltas +58 / +70 / +100 | D2 |
| SC-D-8b | sub-Å-to-active deltas full → depth-8 (Boltz +8.7, OF3 −20.9, Protenix −8.7) — one of the four legs of the lever-versus-degradation split | D3 |
| SC-D-8e | the pLDDT-versus-ln(depth) slopes +0.05 / +0.19 / +0.68 / +0.58 — the leg that identifies *which two* backbones degrade | D3 |
| SC-D-9 | the 5 × 6 Kendall-τ concordance table | D3 |
| SC-D-10 | the OPSD × Boltz cross-tier divergence, 38.8 % at n = 500 against 10.0 % at n = 50 | D1 + D3 |
| SC-D-12 | LPAR1/OF3 helix 60.9 %, Rg 27.9 Å; AGTR1 pocket-Cα moving away from active | D1 + D3 |
| SC-D-8a | the four cluster-boot CIs — but see "not asking", we can regenerate these | D3 |

It also fills the row-count column for Block D in a cross-campaign reference table we
are building (every experiment in both campaigns, with its receptors, partner, ligand
and n). Every other block can print a measured n there. Block D currently prints
"not measurable".

**Delivery.** See the note at the end — short version, by hand in one zip, the way
`rows.tier3.v2.csv` came. If that route is closed, the twelve-column projection in
item 2 is a real substitute and we will take it without complaint.

## 2. If the full files cannot move: a twelve-column projection. **free**

Same shape as the ten-column projection we agreed for `rows.tier3.v2.csv`, adjusted
for the fact that no D-tier arm carries a small-molecule ligand:

```
input_path · receptor_slug · receptor_class · input_state_claim · seed_used ·
sample_idx · confidence_flag · plddt_mean ·
d_npxxy_y558_y753_oh · d_gpcrdb_tm6_tilt_246_637_ca ·
pocket_ca_rmsd_active · pocket_ca_rmsd_inactive
```

`input_path` carries receptor, arm, depth, backbone and seed — GATE-2 §8 gives the
regex you used, `/pool/([^/]+)/([^/]+)/([^/]+)/seed_\d+/` — so nothing else is needed
to reconstruct a cell. If size matters it can be relative to `full/pool/`. Two more
columns if they exist and are cheap to include: the helix fraction and radius of
gyration behind SC-D-12, whatever they are called in the `fold_integrity` group.

This loses SC-D-9's τ table only if the predicate columns are dropped — they are not —
so the projection costs us nothing except the columns we did not think to name.

## 3. One line of text: `head -1` of any one of the three. **free, and it fits in a bridge message**

Four kilobytes of plain text, no attachment, no classifier.

You sent us a real D1 row in `rows_csv_line.txt` and we have reconstructed the schema
from it and from the scorer source, field by field — `redo/protocol/ROWS_SPEC_DERIVED.md`.
It parses to 102 RFC-4180 fields where your covering note said 92 columns, and of those
we can name 33 with certainty, 28 by inference, 31 only from a header in another block,
and **10 not at all**. One header line closes all of it. The ten we cannot name are
fields 3 (`hint`), 8, 9, 10, 64, 66, 67, 68, 91 and the question of whether the trailing
comma means a real 102nd column.

Everything below Rank 1 is smaller than any one of the three items above.

---

# Rank 2 — named analysis artefacts that would let us check specific claims

Each of these is named in your own headline or gate documents as existing on disk.

## 4. The per-cell summaries. **free**

```
experiments/023_tier_d2_directed_inactive/analysis/full/summary_per_cell.csv
experiments/023_tier_d2_directed_inactive/analysis/full/pocket_rmsd_per_cell.csv
experiments/024_tier_d3_msa_depth/analysis/full/summary_per_cell.csv
experiments/024_tier_d3_msa_depth/analysis/full/per_receptor_summary.csv
```

Named in the D2 and D3 headline "Files" sections. GATE-2 §2 records that D3's
`summary_per_cell.csv` matches its own recompute exactly, which is what makes it worth
having: it is a check on the rows rather than a substitute for them. D1 needs no
equivalent — its headline table already prints k/n for all 28 cells, and we have used
those.

These are small. If only one thing in this document can move through a text-sized
channel after item 3, make it these four.

## 5. `experiments/024_tier_d3_msa_depth/analysis/verification/matched_structure_probe_final.json`. **free**

Named in the D3 headline "Files" section. This is the matched-seed 7TM-Cα deviation
behind SC-D-8's leg (d) — 1–4 Å on Boltz against 10–14 on OF3 and 15–20 on Protenix.
It is the leg of the lever-versus-degradation argument that a referee is most likely to
press on, because it is the one that distinguishes "different conformation" from
"worse structure", and it is the only one of the four we cannot approach from the rows
alone without also reconstructing the seed pairing.

## 6. `analysis/block_d/scripts/derive_d3_slopes.py`. **free**

`C-D-2` records that no script produced the four D3 slopes and that GATE-2 grepped the
tree for seven plausible names before refitting them by hand. `ASSUMED_NOT_VERIFIED_BLOCK_D.md`
then says, under the rebuild-and-diff heading, **"What is on disk:
`analysis/block_d/scripts/derive_d3_slopes.py` (this pass, reproduces GATE-2)"**. We
read that as: none existed when GATE-2 ran, and one was written afterwards. If that is
right, it is a fifty-line file that turns a one-off reproduction into a repeatable one,
and we would like it with the rows so our refit and yours are the same refit.

---

# Rank 3 — four sentences and three files

## 7. Four one-line answers. **free**

**(a) OpenFold-3's training cutoff, as a date.** `nanobody_state_anchors.csv`'s fourth
cutoff column is `cutoff_of3_TBD` and `ASSUMED_NOT_VERIFIED_BLOCK_D.md` records that the
release-notes lookup was never done; `pending_curation_tier_d2.csv` carries
`training_cutoff_verified=NO` on all four anchors. Nothing changes either way — 5JQH
(2016), 6VI4 (2020), 4MQS (2013) and 6OS2 (2019) predate any plausible answer — but
C-D-12 is the campaign's largest caveat and we would rather not assert a date nobody
has established. **If you would rather we did the lookup, say so and we will, and send
you the citation for your table.**

**(b) Is `block_d_freeze` placed?** The claim sheet's first line says DRAFT and the tag
not placed, held pending the §2.4 post-cutoff test; Flag D-8 records that the same test
closed on the same day with main-text framing locked. Over the bridge on 2026-09-11 you
gave your HEAD as `10d6493 "Block D bundle manifest v4 (FREEZE)"` and
`paper_af3_release` as `61e6308 "Block D CLOSEOUT"`, and named `block_c_freeze` and
`block_b_final` as placed locally — but not `block_d_freeze`. Is the tag on either
commit? Our Methods currently says the block ships unfrozen, and we would rather it
said frozen and named the commit.

**(c) Are the five shipped D1 CIFs the same samples `PARTA_D1.md §4` measured?**
`MANIFEST.json` gives all five `seed = 1015473677`, `sample_idx = 5`. We checked the
D1 dispatch manifest you sent: 1015473677 is seed-index 4 and is present in all 28
cells, so that is a deliberate fixed-pull rule and not a placeholder — thank you, that
question is answered. What is left is narrower. Measured from the shipped coordinates
the five give 4.22 / 10.09 / 11.31 / 2.61 / 4.91 Å where §4 tabulates 4.19 / 10.03 /
11.58 / 2.25 / 5.42. No state call changes; all five sit on the same side of 9.08.
The two D2 manifest distances reproduce exactly on the same anchors (4.53, and 19.12
against 19.1), and ADRB2 appears in both sets, so the anchors are not the explanation.
The simplest account is that §4 measured a different sample from the same cells, taken
before the 2026-09-10 manifest pull. One sentence either way and we will write it
correctly.

**(d) SC-D-1's four deltas.** The claim sheet gives 98.4 / 60.2 / 98.0 for CNR2, OPSD
and ADRB2 with LPAR1 at 91.6. `PARTA_D1.md §5` gives 97.6 / 59.8 / 96.0 / 91.6 for the
same rule on the same tier, and those four reproduce exactly from the D1 headline's own
k/n counts (499/500 against 11/500, 493/500 against 194/500, 500/500 against 20/500,
459/500 against 1/500). LPAR1 agrees in both; the other three differ by 0.8, 0.4 and
2.0 points. We have written Part A's numbers because they reconcile with the counts.
If the claim-sheet values come from a different pass, which one — so the paper cites it
rather than silently preferring the other document.

## 8. The three random-set structures that failed to pull. **cheap**

```
random_05_d3_b1b1u5_depth8_boltz.cif   seed 2092191293, sample_idx 1
random_06_d3_apj_depth32_boltz.cif     seed 2092191293, sample_idx 7
random_07_d3_npy1r_apo_chai.cif        seed 564473205,  sample_idx 9
```

`MANIFEST.json` preserves the rsync error text for all three, which is exactly the right
way to record a failure — the four that landed carry sha256s and these carry the reason
they do not. A "does the corpus look sane" check on four of an intended seven is worth
re-pulling rather than caveating, and the manifest's `sample_seed = 20260910` makes the
selection reproducible on your side.

## 9. `artefacts/block_d_figure_data_manifest.md`. **free**

Named in the dossier §"SHA-256 manifest at `artefacts/block_d_figure_data_manifest.md`
(analogous to Block C v2.1)". Ten numbered directories arrived with no README and no
data dictionary; the dispatch note supplied a folder map but the zip did not, and the
other three blocks each shipped one. Lowest priority in this document — it costs the
next reader an hour, not a claim.

---

# What we are deliberately NOT asking for

This list is longer than it was on 2026-09-10, because four of the twelve items in that
document turned out to be answerable from the bundle or from data we already hold.

**The bootstrap draws behind SC-D-8's four intervals.** GATE-2 §4 gives the complete
recipe — cluster-bootstrap over the 26 D3 receptors, 1000 replicates, seed 20260910,
receptors resampled with replacement and re-pooled — and §8 records that no file was
written. So the draws do not exist to be sent, and with the rows and the recipe we can
regenerate them. We will also run the paralog-cluster variant GATE-2 §7 flags as
unaddressed, using the `paralogy_clusters.csv` you shipped, and tell you whether the
wider CI changes Chai's verdict.

**A named source for SC-D-11's Block A reproduction.** Withdrawn. `PARTA_D1.md §6`
named it precisely enough and it reproduces here on all 28 cells and all four deltas.

**The seven "missing" D2 cells.** Withdrawn — our arithmetic error, described above.
`W-D-9` and the D2 headline both name the ADRB2 × active_nb drop and its cause.

**Whether a D2 cell is 50 or 200 predictions.** Withdrawn. `C-D-5` argues with itself
inside its own parentheses, but the D2 headline settles it independently: 237 of 240
jobs at 10 samples each, "n=50 per cell except AGTR1×active_nb×of3 (n=20)". Every
interval in the claim sheet reproduces at n = 50 and none at n = 200, which agrees.
We have written 50.

**The phantom scorer SHA `891041e858f3747b`.** W-D-10 and C-D-3 found it, and the
correction to `d9c646af` is yours. We have adopted it and record it only so it is not
re-discovered as though it were new.

**TM6-tilt calibration on the nanobody anchors.** We think we can do this ourselves with
the GPCRdb cache, `bw_numbering.py` and `anchors.py` you already sent. We will tell you
if we cannot before we ask.

**The D2 and D3 dispatch manifests**, the D1 `failure_census.json`, and the D1
`rescore_parallel.provenance.json` — the grids reconcile without the first two and you
already sent us the third.

**Per-receptor D3 slopes.** `PARTA_D3.md §3` breaks them out by family at 2–8 receptors
per cell. We will not build on them.

**Any rerun, any new prediction, anything on a GPU.** The D-tier predictions were run,
scored, audited, and withdrawn from where they did not hold. Nothing in this document
questions that. They simply were not in the zip.

---

# Delivery — what worked last time

`rows.tier3.v2.csv` reached us on 2026-09-12 and verified clean: 91,972,789 bytes,
40,801 rows, sha256 `5ccf58ac…9e2103`, matching the digest pinned in eighteen places
across the Block C drop.

**It came by hand.** Aditya approved it in your session, your platform's outbound
classifier then refused every file regardless of size — `ligand_set.csv` at 33 KB was
denied exactly as the 92 MB one was — and the files came directly instead, as
`paper6f_approved_2026_09_12.zip`. **Nobody on this side looked for a way around that
classifier and nobody will.** If it is still refusing, the same hand route is what we
are asking for: one zip, Aditya carries it.

The ntfy bridge is not a delivery mechanism for these three. It caps attachments at
15 MB and expires them in three hours; D1 alone is 30 MB. The bridge is right for
item 3 (one line of text), items 4 and 6 (small files), and the four answers in item 7 —
those can go as messages today without waiting for anything else.

Landing convention on our side, so you know where it ends up: a late delivery goes to
`analysis/block_d/received_<date>/`, read-only, with its sha256 recorded, rather than
being retro-fitted into `data/block_d/` — that directory is read-only by rule and by
chmod, and adding to it would be editing the drop. That is where `rows.tier3.v2.csv`
sits for Block C.

---

# What we will do with them, so the effort is not blind

Within a day of arrival: `analysis/block_d/verify_claims.py` gains the recomputations
for every claim in the Rank 1 table, the remaining PROSE-ONLY entries collapse to
whatever genuinely cannot be tested, and you get the diff — every number that reproduced
and every number that did not, in the same shape as the Block A, B and C discrepancy
reports. If something disagrees we will send you the recomputation before we write a
sentence about it, and on current form the first suspect will be our checker: of the
eight discrepancies we opened against this block, one is already withdrawn as ours
(D-D-4, which compared a single shipped CIF against a fifty-sample cell median), and
the seven-missing-cells item above makes two.
