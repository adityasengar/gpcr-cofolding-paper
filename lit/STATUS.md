# STATUS.md — moved

The authoritative copy is now **`../STATUS.md`** at the project root, because it
describes experimental blocks, not literature, and both the results ledger
(`../RESULTS.md`) and the drafting stage depend on it.

Read `../STATUS.md`. The rule is unchanged: blocks A, B and C have landed; D1, D2 and
D3 have **not** run; no draft sentence may depend on a planned block.

Numbers in `../STATUS.md` are claims. `../RESULTS.md` records which of them are
actually reproducible from the data in this repo — several are not. Check there before
citing any of them.
