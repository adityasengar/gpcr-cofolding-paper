# W-C-1 — "P5 null holds on all 4 backbones → models don't distinguish agonists from decoys at the ligand interface"

**Retracted**: this framing appeared in the earlier Block C Tier 1
headline draft (`memory: block-c-tier1-headline-2026-09-04`).

**Reason**: **binary-predicate saturation artefact on the Tier 3 cognate
arm.** The two-instrument activation predicate is ceiling-pinned on
cognate-arm cognate-ligand rows across a majority of receptor ×
backbone cells (C-C-1). Under saturation, ANY ligand class registers
as "active" on the binary metric — so a null in that space is not
evidence of the models being blind to ligand identity.

**Correction**: the ligand-class discrimination signal exists on
continuous pocket-Cα-RMSD (SC-C-1 2×2 interaction signed non-zero on
all 4 backbones; SC-C-2 P4 ordinal recovery). Block C reports
continuous readouts for ligand-class claims.

**Cite** in Discussion when a reviewer asks about the earlier framing.
